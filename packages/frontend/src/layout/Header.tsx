import React from 'react';
import styles from '../styles/layout.module.css';

const Header: React.FC = () => {
    return (
        <header className={styles.header}>
            <h1>Side Quest Header</h1>
        </header>
    );
};

export default Header;
